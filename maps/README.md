# 校园地图共享目录

每个学校一个文件夹（用**学校名字**命名），拓扑数据命名为 `topology`（支持 .json / .xlsx），放在自己的文件夹内，不要直接放在 maps/ 根目录：

```
maps/
└── 示例大学/
    └── topology.json
```

拓扑数据获取方式：打开 iClass → 校园地图 → 「导出」（Excel / JSON 都支持），或下载「生成标准模板」后手工填写实测的步行时间。
